/** Browser runtime for presenting the full same-origin report as a compact preview. */
export const LOCAL_REPORT_EMBED_RUNTIME = `const reportFrame=byId("reportFrame");reportFrame.addEventListener("load",()=>{const reportDocument=reportFrame.contentDocument;if(!reportDocument)return;for(const element of reportDocument.querySelectorAll("header,.report-nav"))element.hidden=true;const reportMain=reportDocument.querySelector("main");if(reportMain){reportMain.style.width="min(1600px,calc(100% - 24px))";reportMain.style.margin="16px auto 32px"}const analytics=reportDocument.querySelector(".analytics");if(analytics)analytics.style.marginBottom="12px"});`;
