# SEO Crawl Audit 0.10.3

The local UI now restores the saved HTML report and scan summary after restart,
including previously collected public metrics, without making network requests.

Starting a scan or another metrics update while public metrics are updating now
returns a clear busy response, preventing concurrent report overwrites.

Validated with the full automated quality gate and real Chromium checks for
JavaScript content, redirects, HTTP errors, cancellation, and UI recovery.
